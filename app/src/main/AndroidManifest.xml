<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rahma.antriyuk">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".PanggilActivity"></activity>
        <activity android:name=".PetugasActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DetailActivity" />
        <activity
            android:name=".TabActivity"
            android:label="@string/title_activity_tab"
            android:theme="@style/AppTheme.NoActionBar" /> <!-- <activity android:name=".EditActivity"></activity> -->
        <activity
            android:name=".StrukActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ambilAntrian" />
        <activity
            android:name=".JadwalAnak"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".BerandaActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".DaftarActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>